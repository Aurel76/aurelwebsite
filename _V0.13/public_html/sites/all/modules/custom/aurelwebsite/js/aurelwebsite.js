(function($) {
/*Drupal.behaviors.myBehavior = {
  attach: function (context, settings) {

    //code starts
    $("body").click(function() {
      alert("Hello World");
    });
    //code ends

  }
};*/

jQuery(document).ready(function($) {
  // Code that uses jQuery's $ can follow here.
   /*$("body").click(function() {
      alert("Non mais allo quoi !!");
    });*/
});
})(jQuery);
